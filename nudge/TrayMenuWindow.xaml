<Window x:Class="Nudge.TrayMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nudge menu"
        SizeToContent="WidthAndHeight"
        WindowStyle="None" AllowsTransparency="True" ResizeMode="NoResize"
        ShowInTaskbar="False" Topmost="True" WindowStartupLocation="Manual"
        Background="Transparent" ShowActivated="True"
        TextOptions.TextFormattingMode="Display" UseLayoutRounding="True"
        FontFamily="Segoe UI" FontSize="13">

    <Window.Resources>
        <Style x:Key="MenuRow" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource CardText}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="row" CornerRadius="5"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="row" Property="Background"
                                        Value="{StaticResource CardHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="row" Property="Background"
                                        Value="{StaticResource CardPressed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MenuDivider" TargetType="Border">
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="4,5,4,5"/>
            <Setter Property="Background" Value="{StaticResource CardDivider}"/>
        </Style>
    </Window.Resources>

    <!-- The outer margin is the shadow's room to breathe: the window sizes itself to the whole
         template and clips anything past its edge. -->
    <Border CornerRadius="10" Background="{StaticResource CardSurface}"
            BorderBrush="{StaticResource CardEdge}" BorderThickness="1" Margin="10"
            Effect="{StaticResource CardShadow}">
        <StackPanel x:Name="Rows" Margin="5"/>
    </Border>
</Window>
